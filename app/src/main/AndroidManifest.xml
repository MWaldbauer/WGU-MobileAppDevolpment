<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mwaldbauerscheduler"
    android:versionName="Release">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Activities.AssessmentEditorActivity" />
        <activity android:name=".View.AssessmentListAdapter" />

        <service
            android:name=".MyIntentService"
            android:exported="false" />

        <receiver
            android:name=".MyReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Activities.TermEditorActivity" />
        <activity android:name=".Activities.CourseListActivity" />
        <activity android:name=".Activities.AssessmentListActivity" />
        <activity android:name=".TermListActivity"/>
        <activity android:name=".Activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>